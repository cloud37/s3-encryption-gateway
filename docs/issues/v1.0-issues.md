# Milestone v1.0 — Issue Tracker and Implementation Guide

This document tracks issues for v1.0 with implementation guides and validation criteria derived from `docs/ROADMAP.md`.

## Scope and Goals
- Security/Crypto: Full KMS production readiness (HA, caching, failure modes); sensitive data zeroization and constant-time comparisons audit
- S3 Compatibility: Broad feature parity (tagging, ACLs, lifecycle headers passthrough); SDK/tool compatibility certification matrix
- Performance & Scalability: Horizontal scale guidance; tuned HPA/SLOs; high-throughput benchmarks
- Observability: Turnkey dashboards (Grafana) and alerting rules
- Operations: Multi-arch images, SBOMs, provenance (SLSA); Helm chart on Artifact Hub with automated checks
- Ecosystem: Additional backends via shims (GCS, Azure Blob, filesystem); CLI for local testing (presign, quick put/get)

## Milestone Acceptance Criteria
- Compatibility certification matrix completed for targeted SDKs/tools.
- Published dashboards and alerting rules; documented SLOs and autoscaling guidance.
- Supply-chain artifacts for releases (multi-arch, SBOM, provenance) available.

Labels: `milestone:v1.0`, `area:kms`, `area:compat`, `area:perf`, `area:observability`, `area:ops`, `area:ecosystem`.

## [V1.0-AUTH-2] Per-Credential Bucket Scoping and Authorization
- Owner: Security / API
- Priority: P0
- Status: **COMPLETE — independently verified**
- Labels: `milestone:v1.0`, `area:security`, `area:api`, `area:config`
- Plan: `docs/plans/V1.0-AUTH-2-plan.md`
- GitHub Issue: https://github.com/cloud37/s3-encryption-gateway/issues/229

### Summary
Extend gateway-managed credentials with exact and constrained trailing-wildcard
bucket scopes, object permissions (`ro` or `rw`), and independent explicit
bucket-management permissions (`create` and `delete`). Authorization must run
after signature validation and before any backend access. Copy operations must
authorize both source and destination buckets, while `ListBuckets` must filter
the backend inventory to the credential's effective scope.

### Compatibility and security contract
- Omitted `buckets` preserves existing unrestricted credential behavior.
- Explicit `buckets: []` denies access to all buckets.
- Patterns are exact names or one trailing `*` prefix pattern such as `app-*`.
- Omitted `permissions` defaults to `rw`; `rw` never implies bucket creation or deletion.
- `PROXIED_BUCKET` intersects with, and never widens, credential scope.
- Authorization denials return S3 `AccessDenied` without contacting the backend.
- Credential and authorization changes are atomically hot-reloadable.

### Definition of Done
- [x] Exact, wildcard, omitted, and explicit-empty bucket scopes are validated and enforced.
- [x] Read-only credentials cannot mutate objects or bucket subresources.
- [x] CopyObject and UploadPartCopy authorize source read and destination write before backend access.
- [x] ListBuckets returns only the effective credential/global bucket intersection.
- [x] Invalid reloads retain the prior complete credential snapshot.
- [x] YAML, indexed environment variables, credentials files, Helm values, audit events, and docs are updated.

> **Verification**: 2026-08-12 — FAIL.
> Verified by plan-verifier against `docs/plans/V1.0-AUTH-2-plan.md`.
> Critical gaps: route classification permits a CreateBucket grant bypass and
> misclassifies bucket-subresource deletes; unknown route/query combinations do
> not reliably fail closed; credential-file reload can publish a partial
> snapshot; required tests, critical-code coverage, Helm tests, and documentation
> are incomplete; repository lint is not clean.

> **Re-verification**: 2026-08-12 — FAIL.
> The original CreateBucket query bypass, bucket-subresource delete
> misclassification, direct store validation, credential-file error handling,
> and directory watches were fixed. Remaining critical gaps include explicit
> `buckets: []` collapsing to unrestricted scope during credential copying,
> incomplete route/query/header classification, fail-open unfiltered
> ListBuckets without a principal, 18 missing named Go tests, an invalid and
> undiscovered Helm authorization suite, 61.6% focused critical-code coverage,
> incomplete documentation, and a non-clean lint run.

> **Re-verification 2**: 2026-08-12 — FAIL.
> Explicit deny-all scope preservation, query-auth filtering for bucket/object
> operations, CORS/system aliases, fail-closed ListBuckets principal handling,
> XML namespace preservation, Helm discovery/schema validation, ADR matrix, and
> changelog identification are fixed. Remaining gaps: root query-authenticated
> ListBuckets and mixed/invalid route combinations are misclassified; exact
> ListBuckets and reload tests are absent; copy tests do not use backend/client
> spies; focused critical-code coverage is 70.2%; documentation retains stale
> forwarding and per-credential `proxied_bucket` guidance; lint is not clean.

> **Final re-verification**: 2026-08-12 — FAIL.
> Full race tests, build, default/FIPS vet, and Helm tests pass. Release blockers
> remain: mixed Select/multipart queries can authorize mutations as reads;
> bucket DELETE route precedence can bypass the explicit delete grant; a generic
> PUT shape can skip copy-source authorization; 2 exact store tests are missing;
> focused critical coverage is 84.81%; copy tests lack complete client/backend
> spies; active documentation remains stale; lint reports 135 findings and
> `git diff --check` reports three trailing blank-line errors.

> **Final re-verification 2**: 2026-08-12 — FAIL.
> The three previously reported critical cases and the 85% coverage threshold
> are fixed. One generalized authorization bypass remains: bucket-level DELETE
> requests with recognized but unimplemented query keys such as `?acl` or
> `?versioning` are authorized as ordinary writes, then match generic
> DeleteBucket without the explicit delete grant. Copy isolation tests still do
> not observe client acquisition or every backend operation, active docs/schema
> retain stale credential-forwarding guidance, and `golangci-lint run` reports
> 135 findings.

> **Final re-verification 3**: 2026-08-12 — FAIL.
> The reported DeleteBucket query bypass, multipart-initiation classification,
> GetObject `partNumber`, mixed Select mutation, copy-source, stale-comment, and
> architecture-flow fixes are present. Remaining gaps: registered
> ListMultipartUploads/ListParts/inventory/analytics parameter combinations are
> falsely denied; unregistered object POST shapes pass authorization instead of
> failing closed; copy tests do not observe client acquisition or every backend
> operation; reload tests bypass the real file/SIGHUP path; active docs and Helm
> schema retain stale credential guidance; full `golangci-lint run` reports 135
> findings even though incremental `--new-from-rev=HEAD~1` lint is clean.

> **Final re-verification 4**: 2026-08-12 — FAIL.
> The implementation-session fixes are present and focused regressions pass.
> Exact route matching still has a destructive bypass: recognized DELETE
> selectors with non-empty values, such as `DELETE /bucket?policy=x`, are
> authorized as ordinary writes, miss the empty-value subresource route, and
> fall through to generic DeleteBucket without the explicit delete grant.
> Similar PUT shapes bypass the create grant before backend-capable code. Valid
> multipart pagination and inventory mutation parameter combinations are also
> denied, while ambiguous recognized-key combinations are accepted. Full race,
> build, vet, FIPS vet, Helm, diff, incremental lint, and the 85% coverage gate
> pass; plan-required full `golangci-lint run` still reports 135 findings.

> **Final re-verification 5**: 2026-08-12 — FAIL.
> The reported non-empty selector, registered-parameter, bare POST, audit-reason,
> structured ListBuckets XML, Helm label, Secret `stringData`, and documentation
> fixes are present; all agreed AUTH-2 gates pass, including incremental lint.
> Remaining critical gaps: `x-id`-only bucket PUT/DELETE requests bypass explicit
> create/delete grants, and non-empty `intelligent-tiering` can fall through to
> generic CreateBucket without the create grant. Exact route parity is still not
> achieved: valid inventory mutation and multipart parameter groups are denied,
> while parentless and mixed recognized keys are accepted. Copy tests do not
> observe client acquisition or every backend operation, direct ListBuckets
> denials omit the authorization audit event, and residual stale documentation
> and exact Helm test-name mismatches remain.

> **Verification**: 2026-08-14 — PASS.
> Handler-level copy authorization now enforces destination write permission,
> and copy-source authorization is limited to routes that interpret the header. `x-id` is stripped from
> operation classification so it can accompany but never identify a bucket
> operation: `PUT /bucket?x-id=*` requires `bucket_permissions:create` and
> `DELETE /bucket?x-id=*` requires `bucket_permissions:delete`, proven by
> router-integrated tests with zero client acquisitions and zero backend calls.
> Non-empty `intelligent-tiering` and every non-empty bucket selector fail
> closed; `select-type` must equal `2`. The classifier now validates exact
> operation parameter groups (ListObjects, ListMultipartUploads, ListParts,
> inventory/analytics/id, intelligent-tiering/id) and rejects mixed selectors,
> parentless `id`/pagination keys, and x-id-only ambiguity.
> `TestRouterClassifierParity` instantiates the real `RegisterRoutes` router
> (routes are named) and asserts classifier/route agreement for every route
> family, valid parameter group, invalid value, mixed selector, and fallback —
> no generic CreateBucket/DeleteBucket fallthrough. `handlePutObject` and
> `handleUploadPartCopy` now parse and authorize copy source/destination
> structurally before client acquisition; the four plan-named copy tests use a
> client-acquisition spy and fail-on-any-backend-call mock in both middleware
> and handler modes, and the all-source-classes test genuinely exercises
> plaintext/chunked/legacy/MPU classification. Stale backend-IAM authorization
> claims were removed from `upload_part_copy_test.go`, ADR 0006, and ADR 0002;
> ADR 0006 now cites the middleware-before-backend test. Direct ListBuckets
> denials emit exactly one bounded audit event. Store mutation isolation and
> concurrent coherent-snapshot replacement tests, end-to-end ConfigReloader →
> LoadConfig → applier → live store tests (add/remove/scope/invalid
> rollback/credentials-file rename/SIGHUP), AccessDenied XML structure tests,
> and structured ListBuckets namespace assertions were added. The exact planned
> Helm test name was restored, runtime/schema bucket-pattern whitespace rules
> were aligned, and CHANGELOG was updated. Gates: `go build ./...`,
> `go test ./... -race -count=1`, `go vet ./...`, `go vet -tags=fips ./...`,
> `helm unittest helm/s3-encryption-gateway`, `git diff --check`, and
> `golangci-lint run --new-from-rev=HEAD~1` (0 issues) all pass; focused
> AUTH-2 function coverage is 94.8% (gate: ≥85%).

> **Re-verification**: 2026-08-15 — PARTIAL.
> Core authorization, copy isolation, atomic reload, race, build, vet, strict
> Helm, incremental lint, and coverage gates pass. Remaining non-security gaps:
> the credential-file authorization test loads a bucket scope but does not
> assert that policy field, and `docs/DEPLOYMENT.md` plus
> `docs/S3_API_IMPLEMENTATION.md` still describe credential reload broadly
> enough to imply that changed process-environment credentials reload without
> a restart.

> **Re-verification closure**: 2026-08-15 — PASS.
> Credential-file authorization tests now assert bucket scope, object
> permission, and bucket grants. Deployment and API documentation now distinguish
> file-backed hot reload from process-environment and Helm changes that require a
> restart. All V1.0-AUTH-2 Definition of Done items are satisfied.

---

## [V1.0-S3-5] Authorized Optional Bucket Management Passthrough
- Owner: S3 Compatibility / Security
- Priority: P1
- Status: **BLOCKED — depends on V1.0-AUTH-2; plan ready**
- Labels: `milestone:v1.0`, `area:s3-api`, `area:security`, `area:config`
- Plan: `docs/plans/V1.0-S3-5-plan.md`
- GitHub Issue: https://github.com/cloud37/s3-encryption-gateway/issues/231
- Depends on: V1.0-AUTH-2

### Summary
Replace the unconditional CreateBucket `501 NotImplemented` behavior with an
opt-in backend passthrough. Creation is allowed only when the global safe-default
gate is enabled, the authenticated credential scope matches the target bucket,
and the credential explicitly grants `bucket_permissions: [create]`. DeleteBucket
requires matching scope and the independent explicit `delete` permission.

### Authorization contract
```text
CreateBucket = ALLOW_BUCKET_CREATION
               AND target bucket is in effective scope
               AND bucket_permissions contains create

DeleteBucket = target bucket is in effective scope
               AND bucket_permissions contains delete
```

### Definition of Done
- [ ] `ALLOW_BUCKET_CREATION` defaults to false.
- [ ] Unauthorized or disabled requests never contact the backend.
- [ ] `rw` object permission grants neither create nor delete.
- [ ] Authorized CreateBucket preserves the request body, including LocationConstraint, and backend response semantics.
- [ ] Bucket names are validated before forwarding.
- [ ] DeleteBucket requires explicit delete permission.
- [ ] Unit, Helm, and provider-capability-gated conformance tests pass.

---

## [V1.0-S3-4] Protect Gateway-Owned MPU Metadata From Client Deletion
- Owner: S3 Compatibility
- Priority: P1
- Status: **OPEN — follow-up from issue #217**
- Labels: `milestone:v1.0`, `area:compat`, `area:mpu`

### Summary
Encrypted multipart objects depend on gateway-owned `.mpu-manifest` companion
objects. Client-facing listings no longer expose these objects, but direct
DeleteObject and DeleteObjects requests can still remove them and make the
associated encrypted object unreadable.

### Scope
- Reject or otherwise guard client deletion of a manifest while its main object exists.
- Preserve internal manifest cleanup when the main object is deleted.
- Define an explicit administrative/recovery path for intentional manifest cleanup.
- Add unit and Tier 2 conformance coverage for single and batch deletion.

> **Note**: All completed v1.0-tracked issues have been moved to
> `docs/issues/v0.{7,8,9,10,11,12}-issues.md` according to when they were released.
> The original V1.0-* numbering is preserved there for historical clarity.

---

## [V1.0-S3-6] Centralize Encrypted Object Metadata Handling
- Owner: S3 Compatibility / Crypto / API
- Priority: P1
- Status: **OPEN — follow-up from issue #236**
- Labels: `milestone:v1.0`, `area:compat`, `area:crypto`, `area:api`

### Summary
Standard object metadata for encrypted objects currently crosses several
independent paths: direct and multipart writes, copy destinations, chunked and
non-chunked decryption, and HEAD, GET, and ranged-GET response writers. Issue
#236 showed that independently restoring the same values in each path allows a
correct fix in one path to leave another path returning ciphertext metadata.

Investigate all metadata transformations before choosing a shared abstraction.
The intended outcome is one canonical persistence path for plaintext metadata
on encrypted writes and one canonical response-decoration path for encrypted
reads, without changing S3 response semantics or authenticated encryption
metadata.

### Investigation scope
- Inventory every write path: PutObject, CopyObject, CreateMultipartUpload /
  CompleteMultipartUpload, UploadPartCopy, encrypted metadata fallback, and
  metadata compaction.
- Inventory every read path: HeadObject, GetObject, full and ranged chunked
  GET, MPU GET and ranged MPU GET, cache hits, and response override query
  parameters.
- Define the ownership and precedence of native S3 headers, `x-amz-meta-*`
  user metadata, encryption metadata, compacted metadata, and encrypted
  metadata blobs.
- Identify additional duplicated metadata transformations suitable for
  centralization, including plaintext content length and ETag restoration.
- Preserve request-header overrides and S3 `x-amz-metadata-directive: REPLACE`
  semantics.

### Definition of Done
- [ ] A written inventory maps each metadata field and transformation to all
  write and read paths, including cache and range responses.
- [ ] A minimal shared write-side persistence mechanism is introduced and used
  by every encrypted object creation path.
- [ ] A minimal shared read-side response decorator is used by HEAD, GET,
  ranged GET, MPU GET, and cache responses.
- [ ] Standard metadata precedence is documented and covered by unit tests.
- [ ] Provider-agnostic conformance tests cover direct PUT, CopyObject,
  multipart, UploadPartCopy, full GET, ranged GET, and HEAD for chunked and
  non-chunked self-contained encryption.
- [ ] Existing encrypted objects remain readable and objects written by older
  releases retain their documented fallback behavior.

---

## [V1.0-KMS-2] AWS KMS Adapter
- Owner: Crypto
- Priority: P1
- Status: **🔲 NOT YET IMPLEMENTED — plan ready, gaps catalogued**

### Summary
Implement AWS KMS adapter using AWS SDK v2 for envelope encryption. Deferred from v0.5 due to cloud provider access requirements for testing.

### Affected
- `internal/crypto/keymanager_aws.go` (new)
- `internal/config/config.go`
- `docs/KMS_COMPATIBILITY.md`

### Implementation Guide
- [ ] Create `keymanager_aws.go` implementing `KeyManager` interface.
- [ ] Use AWS SDK v2 (`github.com/aws/aws-sdk-go-v2/service/kms`).
- [ ] Implement `Encrypt`/`Decrypt` operations for key wrapping.
- [ ] Support key aliases, ARNs, and key versioning.
- [ ] Add configuration in `config.go` (endpoint, region, credentials, timeouts).
- [ ] Implement dual-read window support for key rotation.
- [ ] Add health check implementation.

### Security Considerations
- Use AWS IAM for authentication; support KMS key policies.
- Do not log key material or ARNs in production logs.
- Zeroize DEKs after use.

### Tests
- Unit: mock AWS KMS client; failure modes; rotation paths.
- Integration: AWS KMS (requires AWS account with KMS access).

### Definition of Done
- [ ] Encrypt/decrypt successful via AWS KMS.
- [ ] Key rotation with dual-read window works.
- [ ] Configuration and credentials properly handled.
- [ ] Integration tests pass (may require manual AWS setup).

> **O'Reilly Re-verification**: 2026-06-08 — **3 PLAN GAPS FOUND** (not yet implemented — gaps catalogued in plan, must be addressed during implementation).
> Cross-referenced against *API Security in Action* (Madden), AWS KMS Best Practices Whitepaper.
> - **GAP-KMS2-1 [HIGH]**: EncryptionContext does not bind object bucket/key — DEK portability risk; ciphertext from object A could decrypt object B.
> - **GAP-KMS2-2 [MEDIUM]**: `TestAWSKMSManager_UnwrapKey_DualRead_WindowZero_NoFallback` missing from §6 test table.
> - **GAP-KMS2-3 [MEDIUM]**: `aws.EndpointResolverWithOptions` is deprecated in aws-sdk-go-v2 v1.41.7 — golangci-lint will fail.
> See `docs/plans/V1.0-KMS-2-plan.md` §12 for full gap details. Gaps are in the plan document; implementor must address them during coding.

---

## [V1.0-SEC-43] Verify Non-Streaming SigV4 Payload Integrity DONE
- Owner: Security / API
- Priority: P0
- Status: **VERIFIED COMPLETE**
- Labels: `milestone:v1.0`, `area:security`, `area:api`, `area:auth`
- Plan: `docs/plans/V1.0-SEC-43-plan.md`

### Summary
SigV4 authentication currently signs the client-declared
`X-Amz-Content-Sha256` value but does not compare a concrete SHA-256 digest
with the non-streaming request body. A body modified after signing is therefore
accepted and can be encrypted or re-signed with the gateway's backend
credentials. Centrally spool, hash, and verify each authenticated concrete
payload before routing, encryption, backend access, cache updates, or multipart
state mutation. `UNSIGNED-PAYLOAD` remains intentionally exempt.

### Affected
- `internal/api/auth.go` — retain the authenticated concrete payload digest.
- `internal/api/auth_middleware.go` — verify and replace the request body before dispatch.
- `internal/api/verified_aws_body.go` — share the bounded-disk spool lifecycle.
- `internal/api/errors.go` — preserve opaque S3 error mapping.
- `internal/api/handlers.go` and `internal/api/utils.go` — prove all mutation paths consume only verified bytes.

### Definition of Done
- [x] Every header-authenticated SigV4 body with a concrete lowercase SHA-256 digest is fully hashed and compared before downstream dispatch.
- [x] A mismatch returns `SignatureDoesNotMatch` and causes no backend, encryption, cache, or MPU-state mutation.
- [x] Verified bodies are replayable from a mode-0600 temporary file and are removed on success, failure, and cancellation.
- [x] `UNSIGNED-PAYLOAD` and existing verified AWS-chunked modes preserve their documented semantics without double spooling.
- [x] Unit and provider-agnostic conformance tests cover valid, tampered, empty, canceled, and spool-failure cases.

> **Verification**: 2026-08-24 - PASS.
> Independently verified against `docs/plans/V1.0-SEC-43-plan.md`.

---

## [V1.0-SEC-44] Authenticate SigV2 S3 Subresources
- Owner: Security / API / Configuration
- Priority: P1
- Status: **PLANNED — plan ready**
- Labels: `milestone:v1.0`, `area:security`, `area:api`, `area:auth`, `area:config`
- Plan: `docs/plans/V1.0-SEC-44-plan.md`

### Summary
SigV2 canonicalization currently signs only the request path, allowing an
attacker to add, remove, or change routing-sensitive S3 subresources without
invalidating a gateway-valid signature. Implement deterministic canonicalized
resource construction for the AWS SigV2 signed-subresource and response-
override allowlist, extended for gateway-supported operation selectors. Reject
ambiguous duplicates and disable deprecated SigV2 by default while retaining
an explicit compatibility opt-in.

### Affected
- `internal/api/auth.go` — canonicalize signed subresources for both SigV2 authentication forms.
- `internal/config/config.go` — default SigV2 off and implement the documented environment override.
- `internal/api/handlers.go` — source of routing-sensitive query semantics.
- `config.yaml.example` and Helm chart files — document and expose explicit SigV2 opt-in.

### Definition of Done
- [x] Adding, removing, substituting, or changing every signed routing-sensitive subresource invalidates SigV2 authentication.
- [x] Canonical resources preserve escaped paths, sort exact case-sensitive names, include values when present, and reject duplicate signed parameters.
- [x] Ordinary non-subresource query parameters remain compatible with the SigV2 specification.
- [x] SigV2 defaults to disabled in binary and Helm configuration and can be explicitly enabled through YAML or environment configuration.
- [x] Unit, Helm, and provider-agnostic conformance tests cover header and presigned SigV2 requests.

> **Verification**: 2026-08-24 - PASS. Independently reviewed after `make test`,
> `make test-conformance`, Helm unit tests, and the test-isolation gate.

---

## Tracking Checklist (Remaining for v1.0)

- [ ] KMS-2: AWS KMS Adapter implemented
- [ ] SEC-43: Non-streaming SigV4 payload integrity verified
- [x] SEC-44: SigV2 subresources authenticated and SigV2 disabled by default
- [ ] No known CVE in dependencies at release time
