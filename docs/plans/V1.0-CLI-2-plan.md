# V1.0-CLI-2 — Gateway-Aware Audit Tooling (`s3eg-cli`) + Remove Gateway-Bypassing Migration — Implementation Plan

Status: Draft
Owner: DevEx
Priority: P3
Labels: `milestone:v1.0`, `area:ecosystem`, `area:ops`

> Scope clarified relative to the tracker entry at
> `docs/issues/v1.0-issues.md:275`. The tracker entry was rewritten during
> this planning session: the original "extend the migrate tool and rename it,
> retaining all migration functionality" was rejected. This plan instead
> **removes the gateway-bypassing migration write-path entirely** and turns the
> binary into a **pure read-only audit tool** (`s3eg-cli`) with three
> sub-commands (`inspect`, `verify-key`, `list-algorithm`). The supported
> re-encryption path becomes **GET-through-gateway → PUT-through-gateway** using
> any standard S3 tool, backed by a new gateway config flag that closes the
> no-AAD recovery gap. This expands the stub tracker entry into a full
> specification.

---

## 1. Context & Current State

### 1.1 The binary today is a gateway-bypassing migration tool

`cmd/migrate/main.go` builds the binary `s3eg-migrate`. Its `main` dispatches
on `os.Args` (`cmd/migrate/main.go:20-34`): the token `backfill-legacy-no-aad`
routes to `runBackfill()` (`:117`), everything else routes to `runMigrate()`
(`:36`).

`mustBuildDeps` (`cmd/migrate/main.go:178`) is the heart of the bypass: it
loads `gateway.yaml` via `config.LoadConfig` (`:179`), constructs an S3 client
pointed **directly at the backend store** via `s3.NewClient(&cfg.Backend)`
(`:185`), and rebuilds the full crypto stack locally via `buildEngines`
(`:191`, defined `:221`). `buildEngines` calls
`crypto.NewEngineWithChunking(...)` (`:238`) using the operator's password —
i.e. a second, independent copy of the gateway's decryption machinery.

The actual rewrite happens in `internal/migrate`:
- `Migrator.Migrate` (`internal/migrate/migrator.go:75`) lists objects, spins
  up a worker pool (`:151-165`), classifies each via `ClassifyObject`
  (`internal/migrate/classifier.go:71`), and dispatches `migrateJob`s.
- `migrateObject` (`internal/migrate/worker.go:15`) performs the
  decrypt→re-encrypt→**rewrite** cycle: `GetObject` (`:23`),
  `SourceEngine.Decrypt` (`:30`), `TargetEngine.Encrypt` (`:37`),
  buffer-to-temp-file (`:44`), `PutObject` atomic overwrite (`:56`), optional
  `verifyObject` (`:62`), and best-effort fallback-companion `DeleteObject`
  (`:70`).
- `Migrator.BackfillLegacyNoAAD` (`internal/migrate/migrator.go:224`) is a
  metadata-only `CopyObject`-to-self that stamps `MetaLegacyNoAAD="true"`
  (`:292-294`).

This duplicates the gateway's crypto path and must track its evolution
forever — the same liability that rejected V1.0-CLI-1 (per memory: "adding a
bespoke binary for generic operations adds maintenance burden without unique
value").

### 1.2 The gateway already exposes legacy objects

The gateway's `Decrypt` path reads every legacy class through fallbacks. In
particular `internal/crypto/engine.go:802-824` first tries AAD (current and
legacy pipe-delimited formats), then — only when
`expandedMetadata[MetaLegacyNoAAD] == "true"` (`:818`) — attempts a no-AAD
open. Because the gateway can already decrypt these objects, re-encryption
needs no bespoke binary: a `GET` through the gateway yields plaintext and a
`PUT` re-encrypts with the current parameters. Any S3 tool performs this.

### 1.3 The unique value that remains: backend-envelope inspection

What no S3 tool can do is read the **on-backend ciphertext envelope** the
gateway hides. The encryption parameters are stored as object metadata headers
defined in `internal/crypto/engine.go:35-71`:

| Constant | Header | Meaning |
|---|---|---|
| `MetaEncrypted` | `x-amz-meta-encrypted` | `"true"` if encrypted |
| `MetaAlgorithm` | `x-amz-meta-encryption-algorithm` | `AES256-GCM` / `ChaCha20-Poly1305` (`internal/crypto/algorithms.go:12,14`) |
| `MetaKeySalt` | `x-amz-meta-encryption-key-salt` | per-object salt (32 B, `engine.go:30`) |
| `MetaIV` | `x-amz-meta-encryption-iv` | nonce (12 B, `engine.go:31`) |
| `MetaWrappedKeyCiphertext` | `x-amz-meta-encryption-wrapped-key` | KMS-wrapped DEK |
| `MetaKMSKeyID` / `MetaKMSProvider` | `…-kms-id` / `…-kms-provider` | wrapping key identity |
| `MetaKeyVersion` | `x-amz-meta-encryption-key-version` | wrapping key version (`keymanager.go:84`) |
| `MetaLegacyNoAAD` | `x-amz-meta-enc-legacy-no-aad` | AAD scheme marker |
| `MetaKDFParams` | `x-amz-meta-encryption-kdf-params` | KDF algorithm + params |
| `MetaChunkedFormat`/`MetaFallbackMode`/`MetaIVDerivation` | (see `chunked.go:29`, `engine.go:60`, `chunked.go:33`) | format flags used by `ClassifyObject` |

`ClassifyObject` (`internal/migrate/classifier.go:71`) already maps these
headers to a human-readable migration class — directly reusable by the audit
commands.

### 1.4 The no-AAD recovery gap

Removing `BackfillLegacyNoAAD` exposes a gap: objects encrypted before the
`MetaLegacyNoAAD` marker existed (and never backfilled) **fail to decrypt
through the gateway**, because the no-AAD fallback at
`internal/crypto/engine.go:818` is gated on the marker being `"true"`. Such
objects cannot be recovered by the GET→PUT pattern. This plan closes the gap
with a fail-closed gateway config flag (§3.4) rather than retaining the
bypass tool.

### 1.5 Blast radius (acknowledged, in scope)

`s3eg-migrate` is referenced by completed/verified issues and their runbooks:
V1.0-MAINT-1 (`docs/plans/V1.0-MAINT-1-plan.md`), V1.0-MGMT-2 (`--filter=kdf`,
`docs/issues/v0.8-issues.md:7`), V1.0-SEC-H03 (KDF re-seal,
`docs/plans/V1.0-SEC-H03-plan.md:561`), SEC-2/SEC-4 recovery
(`docs/issues/v0.7-issues.md:208`), and `docs/MIGRATION.md` in its entirety.
All must be rewritten to the GET→PUT pattern (§4 Phase F). The objective is to
eliminate a structurally unsafe (gateway-bypassing) maintenance liability while
preserving the genuinely-unique audit capability and a recovery story for every
object class.

---

## 2. Design Goals & Non-Goals

### Goals

1. **Read-only by construction** — the `s3eg-cli` binary issues only
   `ListObjects`, `HeadObject`, and bounded ranged `GetObject` calls. No
   `PutObject`, `CopyObject`, or `DeleteObject` anywhere in the binary or its
   support package. A reviewer can `grep` the package and confirm zero write
   calls.
2. **Migration write-path fully removed** — `Migrate`, `BackfillLegacyNoAAD`,
   `migrateObject`, `verifyObject`, the worker re-encrypt pool, and the
   state/dry-run/verify machinery are deleted. `ClassifyObject` and the
   `ObjectClass` taxonomy are retained (reused by audit commands).
3. **Three audit sub-commands** — `inspect`, `verify-key`, `list-algorithm`,
   each with `--config`, `--log-level`, `--output text|json`.
4. **Binary renamed** — `cmd/migrate` → `cmd/cli`, output binary `s3eg-cli`;
   `s3eg-migrate` retained as a compatibility shim that prints a deprecation
   notice and points at `s3eg-cli` + the GET→PUT pattern.
5. **No-AAD recovery gap closed** — a new gateway config flag
   `encryption.allow_unmarked_no_aad_fallback` (default `false`, fail-closed)
   lets operators temporarily permit no-AAD decryption of unmarked objects so
   GET→PUT can recover them. SEC-4 default behaviour is unchanged when the flag
   is `false`.
6. **Docs realigned** — `docs/MIGRATION.md` rewritten around GET→PUT;
   `docs/S3_CLI_TOOLS.md`, `README.md`, `CHANGELOG.md` updated; all stale
   `s3eg-migrate` runbooks corrected.
7. **Tested** — unit tests (`-race`) for envelope parsing and each sub-command
   over a mock backend; MinIO integration tests behind `//go:build integration`.

### Non-Goals

1. **Generic put/get/presign** — out of scope; covered by `awscli`/`s5cmd`/`mc`
   and by `docs/S3_CLI_TOOLS.md` (same rationale as the V1.0-CLI-1 rejection).
2. **External KMS unwrap (AWS/Vault) in `verify-key`** — `verify-key` validates
   key-version metadata and attempts unwrap via the engine path available from
   `gateway.yaml`; full AWS/Vault unwrap is deferred until KMS adapters
   (V1.0-KMS-2 / V1.0-KMS-3) land. Out of scope here.
3. **In-place re-encryption / metadata-preserving migration** — deliberately
   removed; GET→PUT is the supported path. Edge cases that GET→PUT cannot
   express (exact ETag preservation) are documented as accepted trade-offs.
4. **New crypto formats or algorithms** — none introduced.

---

## 3. Target Interface / Semantics / Behaviour

### 3.1 CLI surface (`s3eg-cli`)

```
s3eg-cli inspect       <bucket> <key>            [--config F] [--log-level L] [--output text|json]
s3eg-cli verify-key    <bucket> <key>            [--key-version N] [--config F] [--log-level L] [--output text|json]
s3eg-cli list-algorithm <bucket>                 [--prefix P] [--workers N] [--config F] [--log-level L] [--output text|json]
```

Dispatch mirrors the existing pattern (`cmd/migrate/main.go:20-34`): inspect
`os.Args[1]` for the sub-command token, build a dedicated `flag.FlagSet` per
sub-command, and require the positional `<bucket>`/`<key>` arguments after flag
parsing. Unknown or missing sub-command prints usage and exits non-zero.
Rationale: keeps the zero-dependency stdlib `flag` style already used; no new
framework (Cobra) is introduced for three read-only commands.

### 3.2 Read-only backend access contract

A reduced client interface replaces `migrate.S3Client`
(`internal/migrate/migrator.go:17`). Only read methods are permitted:

```go
// AuditClient is the read-only backend access surface for s3eg-cli.
// Invariants:
//   - Implementations MUST NOT mutate backend state.
//   - ctx is honoured for cancellation on every call.
type AuditClient interface {
    HeadObject(ctx context.Context, bucket, key string, versionID *string) (map[string]string, error)
    GetObject(ctx context.Context, bucket, key string, versionID *string, rangeHeader *string) (io.ReadCloser, map[string]string, error)
    ListObjects(ctx context.Context, bucket, prefix string, opts s3.ListOptions) (s3.ListResult, error)
}
```

`s3.NewClient(&cfg.Backend)` (existing, `internal/s3/client.go:362`) satisfies
this. Rationale: small-interface guidance — the audit tool advertises, in its
type, that it cannot write.

### 3.3 `inspect` output contract

`inspect` issues `HeadObject` for metadata and a **bounded** ranged
`GetObject` (`Range: bytes=0-N`, N≈64) for the ciphertext fingerprint. It never
reads the full body. Decoded fields:

```go
type EnvelopeReport struct {
    Bucket            string            `json:"bucket"`
    Key               string            `json:"key"`
    Encrypted         bool              `json:"encrypted"`
    Algorithm         string            `json:"algorithm,omitempty"`           // MetaAlgorithm
    Class             string            `json:"class"`                         // ClassToString(ClassifyObject(meta))
    AADScheme         string            `json:"aad_scheme"`                    // "v2-aad" | "v1-no-aad"
    KMSProvider       string            `json:"kms_provider,omitempty"`        // MetaKMSProvider
    KMSKeyID          string            `json:"kms_key_id,omitempty"`          // MetaKMSKeyID
    KeyVersion        string            `json:"key_version,omitempty"`         // MetaKeyVersion
    KDFParams         string            `json:"kdf_params,omitempty"`          // MetaKDFParams
    Chunked           bool              `json:"chunked"`                       // MetaChunkedFormat
    Fallback          bool              `json:"fallback"`                      // MetaFallbackMode
    SaltHex           string            `json:"salt_hex,omitempty"`            // decoded MetaKeySalt
    IVHex             string            `json:"iv_hex,omitempty"`              // decoded MetaIV, with length note
    CiphertextHeadHex string            `json:"ciphertext_head_hex,omitempty"` // first ≤32 bytes
    EncryptionHeaders map[string]string `json:"encryption_headers"`            // all x-amz-meta-encryption-*
}
```

`AADScheme` is derived: `"v1-no-aad"` iff `meta[MetaLegacyNoAAD]=="true"`,
else `"v2-aad"`. Text output is a key-aligned table; JSON output is
`json.MarshalIndent` of the struct. Error table:

| Condition | Exit | Message |
|---|---|---|
| object not found | 3 | `inspect: <bucket>/<key>: not found` |
| not encrypted | 0 | report with `encrypted=false` (informational) |
| metadata salt/IV un-decodable | 0 | field omitted + warn log; not fatal |

### 3.4 Gateway config flag — no-AAD recovery

New field in `EncryptionConfig` (`internal/config/config.go:286`):

```go
// AllowUnmarkedNoAADFallback, when true, permits the gateway to attempt the
// no-AAD decryption fallback for objects that LACK the x-amz-meta-enc-legacy-no-aad
// marker. Default false (fail-closed): preserves the SEC-4 property that an
// attacker with backend write access cannot strip AAD by deleting the marker.
// Operators enable this only during a controlled re-encryption recovery window
// (GET-through-gateway -> PUT-through-gateway).
AllowUnmarkedNoAADFallback bool `yaml:"allow_unmarked_no_aad_fallback" env:"ENCRYPTION_ALLOW_UNMARKED_NO_AAD_FALLBACK"`
```

Engine wiring: add an unexported field `allowUnmarkedNoAAD bool` to `engine`
(`internal/crypto/engine.go:111`), set via a new option
`WithAllowUnmarkedNoAADFallback(bool)` in `engine_options.go`, and relax the
gate at `engine.go:818`:

```go
if expandedMetadata[MetaLegacyNoAAD] == "true" || e.allowUnmarkedNoAAD {
    if pt, err2 := gcm.Open(nil, iv, ciphertext, nil); err2 == nil {
        plaintext = pt
        openErr = nil
    }
}
```

Rationale: fail-closed default keeps the security invariant; the explicit
opt-in is auditable and time-boxed. `cmd/server/main.go` passes the flag into
the engine builder; the migrate-tool path is removed so only the gateway server
honours it.

### 3.5 `verify-key` semantics

Loads `HeadObject` metadata; reports the recorded `MetaKeyVersion`. If
`--key-version N` is supplied, compares against the recorded version and, when
an unwrap path is available from `gateway.yaml` (password/self-contained
engine), attempts a DEK unwrap to confirm; otherwise reports a metadata-only
match with a `verified: "metadata-only"` qualifier in JSON. No full body
decryption. Exit 0 on match, 4 on mismatch, 3 on not-found.

### 3.6 `list-algorithm` semantics

Paginated `ListObjects` + per-object `HeadObject` over a worker pool (default
`--workers 4`, reusing the channel-fan-out shape from
`internal/migrate/migrator.go:143-165` but **without** any write). Aggregates a
`map[algorithm]count` plus a `(plaintext)` bucket and a per-class breakdown.
Output is sorted descending by count with percentages. JSON output:

```json
{"bucket":"b","prefix":"","total":12600,
 "by_algorithm":{"AES256-GCM":12400,"ChaCha20-Poly1305":198,"(plaintext)":2},
 "by_class":{"modern":12598,"class_b_no_aad":2}}
```

---

## 4. Work Breakdown

### Phase A — Package surgery: strip the write-path (`internal/migrate`)
- A1. `internal/migrate/worker.go` — **delete** `migrateObject`,
  `bufferToTempFile`, `verifyObject`. Keep `filterUserMetadata` only if reused
  (it is not after removal → delete).
- A2. `internal/migrate/migrator.go` — **delete** `Migrator` struct, `Migrate`,
  `BackfillLegacyNoAAD`, `probeS3`, `listObjects`, `worker`, `migrateJob`,
  `validateGatewayVersion`, and the write-capable `S3Client` interface
  (`:17-24`). **Keep** `ObjectClass`, `ClassToString`, `ClassifyObject`,
  `NeedsMigration` by moving them into a retained file (A5).
- A3. `internal/migrate/state.go`, `internal/migrate/dryrun.go`,
  `internal/migrate/verify.go` — **delete** (migration-only machinery).
- A4. Corresponding `_test.go` files for deleted code — **delete**
  (`migrator_test.go`, `state_test.go`, `verify_test.go`,
  `migration_test_helpers.go` if migrate-only).
- A5. Rename package `internal/migrate` → `internal/audit` (new dir) **or**
  retain `internal/migrate` housing only the classifier. Decision: rename to
  `internal/audit` to match the new purpose; move `classifier.go` +
  `classifier_test.go` there, adjust package name to `audit`. Add
  `AuditClient` interface (§3.2) here.
- A6. Update both importers of the old package: `cmd/migrate/main.go:16` (now
  the shim, drops the import) and `test/conformance/migration_test.go:23`
  (exercises the removed write-path → delete, or convert to an audit
  conformance test that asserts `inspect`/`list-algorithm` output).

> Phase B depends on the `AuditClient` interface and `ClassifyObject` retained
> in Phase A5.

### Phase B — Audit command implementations (`internal/audit`)
- B1. `internal/audit/inspect.go` — **new** — `func Inspect(ctx, client
  AuditClient, bucket, key string) (*EnvelopeReport, error)`; builds the report
  per §3.3 using metadata constants from `internal/crypto` and `ClassifyObject`.
- B2. `internal/audit/verifykey.go` — **new** — `func VerifyKey(ctx, client
  AuditClient, bucket, key string, wantVersion *int) (*VerifyKeyReport, error)`
  per §3.5.
- B3. `internal/audit/listalgorithm.go` — **new** — `func ListAlgorithm(ctx,
  client AuditClient, bucket, prefix string, workers int) (*AlgorithmReport,
  error)` per §3.6 with a read-only worker pool.
- B4. `internal/audit/report.go` — **new** — `EnvelopeReport`,
  `VerifyKeyReport`, `AlgorithmReport` structs + `WriteText(w io.Writer)` and
  JSON marshalling helpers.

### Phase C — Binary rename + dispatch (`cmd/cli`)
- C1. Move `cmd/migrate/main.go` → `cmd/cli/main.go` (new dir `cmd/cli`).
- C2. `cmd/cli/main.go` — **rewrite** `main` to dispatch `inspect` /
  `verify-key` / `list-algorithm`; remove `runMigrate`/`runBackfill`/
  `mustBuildDeps` migration plumbing; keep `newLogger` (`:200`). Build the
  read-only `s3.NewClient(&cfg.Backend)` and call the Phase B funcs. Each
  sub-command parses its own `flag.FlagSet` with the shared global flags.
- C3. `cmd/cli/inspect.go`, `cmd/cli/verifykey.go`, `cmd/cli/listalgorithm.go`
  — **new** — thin arg-parsing wrappers around `internal/audit` funcs +
  text/json rendering.
- C4. `cmd/migrate/main.go` — **new (shim)** — minimal `main` that prints a
  deprecation notice to stderr (`"s3eg-migrate is deprecated; use s3eg-cli.
  For re-encryption use GET-through-gateway -> PUT-through-gateway, see
  docs/MIGRATION.md"`) and exits non-zero. Retains the build target so existing
  release URLs do not 404.

### Phase D — Gateway config flag (no-AAD recovery)
- D1. `internal/config/config.go:286` — **edit** — add
  `AllowUnmarkedNoAADFallback bool` field (§3.4) to `EncryptionConfig`.
- D2. `internal/crypto/engine.go:111` — **edit** — add `allowUnmarkedNoAAD
  bool` field to `engine`.
- D3. `internal/crypto/engine_options.go` — **edit** — add
  `WithAllowUnmarkedNoAADFallback(b bool) Option`.
- D4. `internal/crypto/engine.go:818` — **edit** — relax the no-AAD gate per
  §3.4.
- D5. `cmd/server/main.go` — **edit** — pass
  `crypto.WithAllowUnmarkedNoAADFallback(cfg.Encryption.AllowUnmarkedNoAADFallback)`
  into the server engine construction at the existing
  `crypto.NewEngineWithOpts(...)` call (`cmd/server/main.go:591`, alongside the
  option list that includes `WithPBKDF2Iterations` at `:578`).
- D6. `config.yaml.example` + `helm/s3-encryption-gateway/values.schema.json`
  (and `values.yaml`) — **edit** — document the new flag (default false).

### Phase E — Build & CI
- E1. `Makefile` — **edit** — rename targets `migrate`→`cli`,
  `migrate-multiarch`→`cli-multiarch`; output `bin/s3eg-cli-*`; build `cmd/cli`.
  Keep a `migrate` alias target that builds the shim binary `bin/s3eg-migrate-*`.
  Update `help` text (`Makefile:526-527`).
- E2. `.github/workflows/helm.yml:127-129` — **edit** — build/upload
  `s3eg-cli-*` artifacts from `./cmd/cli`; add shim `s3eg-migrate-*` build
  pointing at `./cmd/migrate`.

### Phase F — Documentation realignment
- F1. `docs/MIGRATION.md` — **rewrite** — replace the offline-tool runbook with
  the GET→PUT-through-gateway re-encryption pattern; add the
  `encryption.allow_unmarked_no_aad_fallback` recovery procedure for
  pre-marker no-AAD objects; reference `s3eg-cli inspect`/`list-algorithm` for
  pre/post audit.
- F2. `docs/S3_CLI_TOOLS.md` — **edit** — update `s3eg-migrate` references
  (lines ~567-623) to `s3eg-cli`; correct the audit-tool examples.
- F3. `README.md` — **edit** — lines 185 and 874: replace `s3eg-migrate`
  migration references with the GET→PUT pattern + `s3eg-cli` audit tool.
- F4. `CHANGELOG.md` — **edit** — add **v1.0 – Unreleased** entry for
  V1.0-CLI-2 (rename, write-path removal, audit commands, config flag).
- F5. Stale runbook references in completed-issue plans
  (`docs/plans/V1.0-MAINT-1-plan.md`, `V1.0-SEC-H03-plan.md`,
  `docs/issues/v0.7-issues.md:151,208`, `v0.8-issues.md`) — **edit** — append a
  note that the offline tool is superseded by GET→PUT (do not rewrite history;
  add a forward-pointer line).

### Phase G — Tests
- G1. `internal/audit/inspect_test.go` — **new** — table-driven over mock
  metadata maps (modern, class_b_no_aad, fallback, plaintext, KMS-wrapped).
- G2. `internal/audit/verifykey_test.go` — **new** — match / mismatch /
  metadata-only.
- G3. `internal/audit/listalgorithm_test.go` — **new** — mock paginated list,
  concurrency under `-race`.
- G4. `internal/crypto/engine_test.go` (or new `sec4_allowflag_test.go`) —
  **new/edit** — flag-off keeps SEC-4 fail-closed; flag-on recovers an unmarked
  no-AAD object.
- G5. `test/audit_integration_test.go` — **new** — `//go:build integration`,
  MinIO via Testcontainers: write encrypted objects through the gateway, then
  assert `inspect`/`verify-key`/`list-algorithm` outputs.

---

## 5. Affected Files

### Direct edits
- `internal/config/config.go` — add `AllowUnmarkedNoAADFallback`.
- `internal/crypto/engine.go` — add engine field; relax no-AAD gate (`:818`).
- `internal/crypto/engine_options.go` — add option.
- `cmd/server/main.go` — wire the new option.
- `internal/migrate/migrator.go`, `worker.go` — delete write-path; move
  classifier out.
- `Makefile` — rename build targets; add shim alias.
- `.github/workflows/helm.yml` — build `s3eg-cli` + shim.
- `config.yaml.example`, `helm/.../values.yaml`, `helm/.../values.schema.json`
  — document flag.
- `docs/MIGRATION.md`, `docs/S3_CLI_TOOLS.md`, `README.md`, `CHANGELOG.md` —
  realign.
- `docs/plans/V1.0-MAINT-1-plan.md`, `docs/plans/V1.0-SEC-H03-plan.md`,
  `docs/issues/v0.7-issues.md`, `docs/issues/v0.8-issues.md` — forward-pointers.

### New files
- `cmd/cli/main.go`, `cmd/cli/inspect.go`, `cmd/cli/verifykey.go`,
  `cmd/cli/listalgorithm.go` — renamed binary + sub-command wrappers.
- `cmd/migrate/main.go` — deprecation shim (replaces deleted migration `main`).
- `internal/audit/classifier.go` (+ `classifier_test.go`) — moved from
  `internal/migrate`.
- `internal/audit/inspect.go`, `verifykey.go`, `listalgorithm.go`, `report.go`
  — audit logic + `AuditClient` interface.
- `internal/audit/inspect_test.go`, `verifykey_test.go`,
  `listalgorithm_test.go` — unit tests.
- `test/audit_integration_test.go` — MinIO integration tests.

### Deleted files
- `internal/migrate/state.go`, `dryrun.go`, `verify.go` and their `_test.go`;
  `internal/migrate/migrator_test.go`, `state_test.go`, `verify_test.go`,
  migration-only helpers.

---

## 6. Test Strategy

| Layer | Tests |
|---|---|
| Unit — inspect | `TestInspect_ModernObject_ReportsAES256GCM` — asserts algorithm/class/AAD fields from mock metadata |
| Unit — inspect | `TestInspect_NoAADObject_ReportsV1NoAAD` — `MetaLegacyNoAAD=true` → `aad_scheme=v1-no-aad`, class `class_b_no_aad` |
| Unit — inspect | `TestInspect_Plaintext_ReportsEncryptedFalse` — non-encrypted object |
| Unit — inspect | `TestInspect_KMSWrapped_ReportsProviderKeyVersion` — `MetaKMSProvider`/`MetaKeyVersion` surfaced |
| Unit — inspect | `TestInspect_MalformedSalt_OmitsFieldNotFatal` — undecodable `MetaKeySalt` → field omitted |
| Unit — verify-key | `TestVerifyKey_MatchingVersion_ReturnsMatch` |
| Unit — verify-key | `TestVerifyKey_MismatchVersion_ReturnsMismatch` (exit-code mapping) |
| Unit — verify-key | `TestVerifyKey_NoUnwrapPath_ReturnsMetadataOnly` |
| Unit — list-algorithm | `TestListAlgorithm_AggregatesCountsAndPct` — mock paginated list |
| Unit — list-algorithm | `TestListAlgorithm_Concurrent` — worker pool correctness under `-race` |
| Unit — config | `TestEncryptionConfig_AllowUnmarkedNoAAD_DefaultsFalse` |
| Unit — engine | `TestDecrypt_UnmarkedNoAAD_FlagOff_FailsClosed` — SEC-4 regression |
| Unit — engine | `TestDecrypt_UnmarkedNoAAD_FlagOn_Recovers` |
| Unit — readonly guard | `TestAuditClient_InterfaceHasNoWriteMethods` — compile-time/reflective assert there is no `PutObject`/`CopyObject`/`DeleteObject` |
| Integration | `TestAudit_MinIO_InspectVerifyList_EndToEnd` — `//go:build integration`, Testcontainers MinIO |

Commands to verify locally:

```bash
go build ./...
go vet ./...
go test ./internal/audit/... ./internal/crypto/... -race -count=1
go test ./cmd/cli/... -race -count=1
go test -tags=integration ./test/... -run TestAudit_MinIO   # requires Docker
golangci-lint run
helm lint helm/s3-encryption-gateway
```

---

## 7. Risks & Mitigations

| Risk | Mitigation |
|---|---|
| `allow_unmarked_no_aad_fallback` becomes a permanent AAD-bypass, weakening SEC-4 | Default `false`; documented as a time-boxed recovery window; regression test `TestDecrypt_UnmarkedNoAAD_FlagOff_FailsClosed`; promoted to DoD §8. |
| Removing the migration write-path strands objects the gateway cannot decrypt | The new flag closes the only known gap (pre-marker no-AAD); `inspect`/`list-algorithm` let operators enumerate affected objects before the window. |
| Existing release automation / operators expect `s3eg-migrate` binary | Keep the binary name as a deprecation shim that exits non-zero with guidance; CI still publishes it; transition documented in CHANGELOG. |
| Deleting `internal/migrate` breaks importers elsewhere | Two importers exist: `cmd/migrate/main.go:16` and `test/conformance/migration_test.go:23`. Move `ClassifyObject` to `internal/audit`, update both imports, and delete/port the conformance migration test (it exercises the removed write-path; convert to an `inspect`/`list-algorithm` audit conformance test or delete with rationale). |
| `inspect` ranged GET reads more than intended | Use an explicit `Range: bytes=0-63` header; cap the fingerprint at 32 decoded bytes; never call `io.ReadAll` on the full body. |
| Stale runbooks in verified issues mislead operators | Phase F adds forward-pointer notes; `docs/MIGRATION.md` fully rewritten; DoD requires all `s3eg-migrate` migration references corrected. |

---

## 8. Definition of Done

Per `docs/issues/v1.0-issues.md:275`.

- [x] `inspect`, `verify-key`, `list-algorithm` implemented in `internal/audit`
      and exposed via `cmd/cli`; all read-only.
- [x] Migration write-path removed: `grep -r "func (m \*Migrator)"`,
      `migrateObject`, `BackfillLegacyNoAAD` return nothing; no `PutObject`,
      `CopyObject`, or `DeleteObject` call exists in `cmd/cli` or
      `internal/audit`.
- [x] `AuditClient` interface exposes only read methods (compile-time test
      `TestAuditClient_InterfaceHasNoWriteMethods` passes).
- [x] Binary renamed to `s3eg-cli` (`cmd/cli`); `s3eg-migrate` shim prints a
      deprecation notice and exits non-zero; `make cli` and `make migrate`
      both build.
- [x] `encryption.allow_unmarked_no_aad_fallback` config field added, defaults
      `false`, documented in `config.yaml.example` + Helm schema; engine honours
      it (`engine.go:818`).
- [x] SEC-4 fail-closed preserved when flag is `false`
      (`TestDecrypt_UnmarkedNoAAD_FlagOff_FailsClosed` passes); flag-on recovery
      proven (`TestDecrypt_UnmarkedNoAAD_FlagOn_Recovers`).
- [x] All sub-commands emit valid `--output json`.
- [x] `go build ./...`, `go vet ./...` clean; unit tests pass under
      `-race -count=1`.
- [ ] MinIO integration test `TestAudit_MinIO_InspectVerifyList_EndToEnd`
      passes under `-tags=integration` (may require Docker/CI).
- [x] `Makefile` and `.github/workflows/helm.yml` updated; CI build green.
- [x] `docs/MIGRATION.md` rewritten to the GET→PUT pattern incl. the no-AAD
      recovery flag; `docs/S3_CLI_TOOLS.md`, `README.md`, `CHANGELOG.md`
      updated; stale `s3eg-migrate` runbook references in verified-issue docs
      carry forward-pointer notes.
- [x] `helm lint` clean.

---

## 9. Milestones & Estimated Effort

| Phase | Output | Effort |
|---|---|---|
| A | Strip write-path; move classifier to `internal/audit` | 0.75 d |
| B | Three audit functions + report types | 1.0 d |
| C | `cmd/cli` rename + dispatch + sub-command wrappers + shim | 0.75 d |
| D | `allow_unmarked_no_aad_fallback` config + engine + server wiring | 0.5 d |
| E | Makefile + CI rename | 0.25 d |
| F | Docs rewrite (`MIGRATION.md` et al.) | 0.75 d |
| G | Unit + integration tests | 1.0 d |
| **Total** | | **5.0 d** |

---

## 10. Follow-ups / Out-of-scope items surfaced during planning

1. **External KMS unwrap in `verify-key`** — once V1.0-KMS-2 (AWS) /
   V1.0-KMS-3 (Vault) land, extend `verify-key` to unwrap against the
   configured external KMS. Tracked separately.
2. **Automatic batch GET→PUT helper** — a documented shell loop is shipped in
   `docs/MIGRATION.md`; a small `s3eg-cli reencrypt-via-gateway` *that calls the
   gateway endpoint* (not the backend) could be a future convenience, but is
   intentionally excluded here to avoid reintroducing put/get logic.
3. **Drop the `s3eg-migrate` shim entirely in v2.0** — after a deprecation
   period, remove `cmd/migrate` and its CI build step.

---

## 11. References

1. Alan A. A. Donovan & Brian W. Kernighan, *The Go Programming Language*
   (Addison-Wesley, 2015) — small-interface design and `flag`-based CLI
   dispatch idioms applied in §3.1–§3.2.
   <https://learning.oreilly.com/library/view/-/9780134190440/>
2. Jon Bodner, *Learning Go*, 2nd ed. (O'Reilly, 2024) — worker-pool fan-out
   with channels and `context` cancellation patterns reused for
   `list-algorithm` (§3.6).
   <https://learning.oreilly.com/library/view/-/9781098139285/>
3. NIST SP 800-38D, *Recommendation for Block Cipher Modes of Operation:
   Galois/Counter Mode (GCM) and GMAC* — AEAD/AAD integrity rationale behind
   the fail-closed default of `allow_unmarked_no_aad_fallback` (§3.4, §7).
   <https://csrc.nist.gov/pubs/sp/800/38/d/final>
4. Aaron Parecki et al., security guidance on authenticated encryption and
   metadata integrity — basis for not letting backend-writable metadata relax
   AEAD verification by default (§3.4).
   <https://learning.oreilly.com/library/view/-/9781098143619/>
